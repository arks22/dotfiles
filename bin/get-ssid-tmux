#!/bin/sh

ssid=$(/System/Library/PrivateFrameworks/Apple80211.framework/Versions/Current/Resources/airport -I \
  | grep " SSID" \
  | awk '{$1="";print}' \
  | sed "s/ //")

[[ -n "$ssid" ]] && str="#[fg=colour014]$ssid" || str="#[fg=red]✘"

echo "#[bold][ $str #[bold,fg=default]]#[default] "
